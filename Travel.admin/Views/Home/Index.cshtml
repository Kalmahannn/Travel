@model List<Hotel>

@{
    ViewData["Title"] = "Справочник Hotel";
    ViewData["Description"] = "список Hotel";
}
<table class="table table-striped table-bordered table-advance table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Location</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (Hotel item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Location</td>
                <td>
                    <a href="@Url.Action("EditHotel", "Home", new{id=item.Id})">edit</a>
                </td>
                <td>
                    <a href="@Url.Action("DeleteHotel", "Home", new{id=item.Id})">delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<hr />
<a href="@Url.Action("CreateHotel", "Home")" class="btn btn-default">Add Hotel</a>